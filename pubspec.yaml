name: timeman_app
description: Mobile application for mobile-programming course

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  provider:
  google_fonts:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  shared_preferences:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
